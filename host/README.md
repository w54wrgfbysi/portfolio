# Updated private artwork host

Changes requested:
- Removed the top-left "Private artwork host" text.
- Removed the top-right "Available for viewing" status.
- Fixed the DMCA wording so "DMCA takedown request" stays inline instead of breaking into a vertical block.
- Corrected the copyright mark sizing and alignment.
- Removed the bottom-right "Private viewing only" footer item.

Open `index.html` in a browser. Replace `assets/artwork.png` with your own image and edit the metadata values in the HTML.
